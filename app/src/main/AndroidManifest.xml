<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.com.example.vansh.wdyw">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ppp"
        android:label="@string/WDYW"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/WDYW"
            android:theme="@style/My">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity><activity android:name=".activity.MainActivity" android:label="Main Activity"></activity>
        <activity android:name=".activity.StartupActivity" android:label="Startup Activity"></activity>
        <activity android:name=".activity.Profile" android:label="Profile"></activity>
        <activity android:name=".activity.LoginActivity" android:label="Login"></activity>
        <activity android:name=".activity.SignupActivity" android:label="SIGN UP"></activity>


    </application>

</manifest>
